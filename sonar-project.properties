# Required project identification
sonar.projectKey=app-repo
sonar.projectName=App Repo
sonar.projectVersion=1.0

# Source code location
sonar.sources=.

# Language (optional, based on your code – adjust if needed)
sonar.language=py

# Exclude tests or virtual environments if needed
sonar.exclusions=**/venv/**,**/__pycache__/**

# Encoding of the source code
sonar.sourceEncoding=UTF-8

# If using a specific linter like pylint or coverage, you can add reports:
# sonar.python.pylint.reportPath=reports/pylint.json
# sonar.python.coverage.reportPaths=coverage.xml
